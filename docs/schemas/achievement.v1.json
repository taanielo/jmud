{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "jmud:achievement:v1",
  "title": "Achievement (v1)",
  "description": "A single milestone achievement loaded from data/achievements/<id>.json. An achievement unlocks the moment the player's tracked statistic (condition) reaches its threshold. The loader tolerates unknown properties (AGENTS.md §11).",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "id",
    "name",
    "condition",
    "threshold"
  ],
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 1
    },
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9_]+$",
      "description": "Unique achievement id, also used as the persisted key on the player record."
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "Human-readable display name shown in the ACHIEVEMENTS command."
    },
    "description": {
      "type": "string",
      "description": "Short flavour description of how the achievement is earned."
    },
    "condition": {
      "type": "string",
      "enum": [
        "total_kills",
        "level",
        "quests_completed"
      ],
      "description": "The player statistic tracked toward this achievement: total_kills (cumulative mob kills), level (character level), or quests_completed (cumulative distinct one-time quests completed)."
    },
    "threshold": {
      "type": "integer",
      "minimum": 1,
      "description": "The value the condition must reach for the achievement to unlock."
    },
    "title_reward": {
      "type": "string",
      "minLength": 1,
      "description": "Optional title granted to the player when this achievement unlocks (same shape as a quest title_reward). Must be unique across achievements and quests, case-insensitively. Omit when the milestone grants no title."
    }
  }
}
