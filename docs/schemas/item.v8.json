{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "jmud:item:v8",
  "title": "Item (v8)",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "id",
    "name",
    "description",
    "attributes",
    "effects",
    "messages",
    "weight",
    "value"
  ],
  "properties": {
    "schema_version": {
      "type": "integer",
      "const": 8
    },
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9-]+$"
    },
    "name": {
      "type": "string",
      "minLength": 1
    },
    "description": {
      "type": "string",
      "minLength": 1
    },
    "attributes": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "stats"
      ],
      "properties": {
        "stats": {
          "type": "object",
          "additionalProperties": {
            "type": "integer"
          }
        }
      }
    },
    "effects": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "effect_id",
          "duration_ticks"
        ],
        "properties": {
          "effect_id": {
            "type": "string",
            "pattern": "^[a-z0-9-]+$"
          },
          "duration_ticks": {
            "type": "integer",
            "minimum": 0
          },
          "op": {
            "type": "string",
            "enum": ["APPLY", "REMOVE"],
            "description": "Whether the item applies the effect (default) or removes it (a cure)."
          }
        }
      }
    },
    "messages": {
      "type": "array"
    },
    "equip_slot": {
      "type": "string",
      "enum": [
        "weapon",
        "offhand",
        "head",
        "chest",
        "legs",
        "hands",
        "feet"
      ]
    },
    "weight": {
      "type": "integer",
      "minimum": 0
    },
    "value": {
      "type": "integer",
      "minimum": 0
    },
    "attack_ref": {
      "type": "string",
      "pattern": "^[a-z0-9.-]+$",
      "description": "Id of the attack used when this item is equipped as a weapon."
    },
    "teaches_ability_ref": {
      "type": "string",
      "pattern": "^[a-z0-9.-]+$",
      "description": "Id of the ability learned by the player when this item (a scroll) is read."
    },
    "container_capacity": {
      "type": "integer",
      "minimum": 1,
      "description": "Maximum number of items this container can hold. Present only on container items (bags, chests, strongboxes); absent for normal items."
    },
    "contents": {
      "type": "array",
      "description": "Items currently stored inside this container. Absent or empty for empty containers and non-container items; contained items are never themselves containers.",
      "items": {
        "$ref": "#"
      }
    },
    "light_radius": {
      "type": "integer",
      "minimum": 1,
      "description": "Radius of light this item emits while carried. Present only on light-source items (torch = 1, lantern = 2); absent for non-light-emitting items. A carried light source lets its bearer see in dark rooms whose required light radius it meets or exceeds."
    },
    "max_durability": {
      "type": "integer",
      "minimum": 1,
      "description": "Maximum durability of this equippable item. Present only on breakable gear; absent for items that never wear out. When present, the item loses durability as its wearer takes damage in combat and can be restored by a blacksmith's REPAIR."
    },
    "durability": {
      "type": "integer",
      "minimum": 0,
      "description": "Current durability, from 0 (broken, unusable in combat) up to max_durability. Absent means the item starts at full durability. Only meaningful when max_durability is present."
    },
    "rarity": {
      "type": "string",
      "enum": ["common", "uncommon", "rare"],
      "description": "Rarity tier of this item, governing its colored display name and which stat affixes it may carry. Absent means 'common' (no visual highlight and no affixes), keeping legacy item data fully backward compatible."
    },
    "affixes": {
      "type": "array",
      "description": "Ids of the stat affixes attached to this item (see data/item-affixes.json). Each affix contributes additive stat bonuses combined with attributes.stats when computing effective stats. Absent or empty for plain items.",
      "items": {
        "type": "string",
        "pattern": "^[a-z0-9-]+$"
      }
    }
  }
}
